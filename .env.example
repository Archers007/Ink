# Port for the local server.
PORT=3000

# Cloudflare Tunnel token (for the `cloudflared` service in docker-compose).
# Get it from the Cloudflare Zero Trust dashboard. NEVER commit the real value.
CLOUDFLARED_TOKEN=
